# URL backend FastAPI
# Development (via Vite proxy): gunakan /api
# Production: ganti dengan URL deployment Render/Railway
VITE_API_BASE_URL=/api

# Nama aplikasi (tampil di tab browser & sidebar)
VITE_APP_NAME=Presensi SKS — Admin
